<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>
  <link rel="stylesheet" href="/css/main.css" />
</head>
<body class="bg-[#f5f8fa]">
<div class="max-w-[1200px] mx-auto flex flex-col min-h-screen">

<header class="bg-white border-b p-4 flex justify-between">
  <h1 class="font-bold text-xl">Task Manager</h1>

  <% if (user) { %>
    <div>
      <span class="mr-3"><%= user.username %></span>
      <a href="/logout" class="text-blue-500">Logout</a>
    </div>
  <% } %>
</header>

<div class="flex flex-1">
